export default function SongCard(){

    {/*  SongCard
    return (
        <div className="album-card">
            {albums.map(album => (
                <div key={album.key} className="album-card">
                    <img src={album.cover_medium} alt={album.title}></img>
                    <h3>{album.title}</h3>
                    <p>{album.artist.name}</p>
                </div>
            ))}
        </div>
    );
    */}
};

